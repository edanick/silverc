import * from fs;
void main() {
    printline("A");
    string inputFile = "compiler_driver.sr";
    string outputFile = "silver";
    string source = "fake source for testing";
    printline("B");
    printline("read");
    printline(source);
    printline("tokens done");
    printline("parsed");
    printline("codegen done");
    printline("pe built");
    printline("written");
}
