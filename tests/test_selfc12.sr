class Foo {
    string name;
    int val;

    void setup(string n, int v) {
        this.name = n;
        this.val = v;
    }

    int getVal() {
        return this.val;
    }
}

void main() {
    Foo f = new Foo();
    f.setup("hello", 42);
    printline("val: " + f.getVal());
}
