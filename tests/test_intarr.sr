mod export compiler;

class Foo {
    public prop int[] buf;

    construct () {
        this.buf = [];
    }

    public int[] getBytes() {
        return this.buf;
    }

    public void addImport(string dll, string name) {
    }

    public int[] build(int[] stringData) {
        return stringData;
    }
}

void main() {
    Foo f = new Foo();
    int[] b = f.getBytes();
}
