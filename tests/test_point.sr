class Point {
    public prop int x;
    public prop int y;

    construct (int x, int y) {
        this.x = x;
        this.y = y;
    }
}

void main() {
    Point p;
    p = new Point(3, 4);
    printline(p.x);
    printline(p.y);
}
