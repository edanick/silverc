import * from fs;
void main() {
    testExists();
}
void testExists() {
    FileOps file;
    bool ex = file.exists("simple.sr");
    if (ex) { printline("exists=true"); }
    else { printline("exists=false"); }
}
