mod export compiler;
import * from fs;

void main() {
    int[] arr = [1, 2, 3];
    arr.add(4);
    arr.add(5);
    printline(arr.length);
    for (int i = 0; i < arr.length; i++) {
        printline(arr[i]);
    }
}
