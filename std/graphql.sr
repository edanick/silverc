import * from win32.msvcrt;

mod export graphql;
import {sleep} from time;

public const int Int = 1;
public const int String = 2;
public const int Float = 3;
public const int Boolean = 4;

class args {
    public string get(string name) { return ""; }
}

class ctx {
    public object request;
}

class node {
    public string id;
    public string name;
    public string email;
}

class result {
    public object data;
}

class schema {
    public void type(string name, object fields) { }
    public void query(string name, void callback(args, ctx)) { }
}

class client {
    private string url;

    construct(string url) { this.url = url; }

    public void header(string name, string value) { }

    public result query(string q) {
        result r = new result();
        r.data = {users: {}, user: {}};
        return r;
    }

    public result mutate(string q, object variables) {
        result r = new result();
        return r;
    }
}

class server {
    construct(schema s, int port) { }
    public void start() { }
    public void wait() { while (true) { time.sleep(1000); } }
}
